Add a leg: for stuff bits: stuff(D): dynamic stuff bit

Improve colors of input fields => blue is to dark

If Fields are disabled: hide the field

Vertical space between input fields: align them.

--------------------------
Expert configuration: add bit rate and SP for Arb & Data => VHDL file can be downloaded

I want an option, to also draw the bits with real proportianal bit lenghts. Since this is an expert feature it has to be first enabled. The user will enter the bit rate in the arbitration phase and the data phase, and also both Sample Point position.

In index.html
- Add a slider like in iPhones to select "Real bit ratio": ON/OFF.
If slider set to "ON", the following additional configuration input parameters appear below the ACK slot input: Two lines with each two input parameters.
- Arbitration: Bit-rate [ 500] kbit/s, SP [80]%
- Data:        Bit-rate [1000] kbit/s, SP [70]%
The values in paranthesis are the default values.

In frame_definitions.js
- add new inputs to variable bitTimeInfo, when realBitRatio = ON.
  - realBitRatio = true (false when slider set to OFF)
  - realArbDataBitLenRatio = data/arbitration (calculated from parameters)
  - dataSP in %
  - arbSP in %

In frame_draw.js
Draw bits according the given bit ratio.
In case of CAN FD, the bit where the data phase starts and ends is special.
For the bit, where the data phase starts: Up to the Sample Point (SP) the bit is an Arbitration bit, from the SP the bit is a data bit.
For the bit, where the data phase ends: Up to the Sample Point (SP) the bit is an Data bit, from the SP the bit is a arbitration bit.
Show a vertiacal separtor line at the SP, with half the hight.

In frame_export.js
asdf